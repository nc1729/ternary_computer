

main:
    SET A, 1
    SET B, 1
    SHOW A
    SHOW B
    SET C, 8 # only need to loop 8 times
    SET E, 0

    !loop
        ADD A, B
        SHOW A
        DEC C
        CMP C, E
        JPZ fin
    JP loop

    !fin

    STRPNT "All done!\n"

end main